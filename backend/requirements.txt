# Runtime dependencies for the OILTRACE-AI backend (API + ML pipeline).
# PyTorch is installed separately (CPU wheel) in the Docker build; locally any
# torch install works. See Dockerfile / README.
numpy==2.1.3
pandas==2.2.3
scipy==1.15.3
scikit-learn==1.6.1
xgboost==3.4.1
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.10.3
pydantic-settings==2.6.1
sqlalchemy==2.0.39
python-dotenv==1.0.1
rasterio==1.5.1
shapely==2.1.2
Pillow==11.1.0

# Optional (production drift engine; large, GDAL-dependent):
# opendrift==1.12.0
# psycopg[binary]==3.2.1   # PostGIS production DB driver, plus Postgres/PostGIS server