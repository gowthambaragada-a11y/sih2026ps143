# OILTRACE-AI · PS143 backend — runtime dependencies.
# Run from backend/:  pip install -r requirements.txt

# --- Web framework ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.20      # multipart/form-data UploadFile
requests==2.32.3              # fetching SAR images by URL

# --- ML / SAR detection (uses modules/sar_unet UNetTiny) ---
torch==2.14.0+cpu             # or drop the +cpu suffix for a CUDA build
torchvision==0.24.0+cpu
numpy==2.1.3
Pillow==11.1.0
scipy==1.15.3
scikit-image==0.25.0          # image morphology + contour extraction
shapely==2.1.2                # polygon simplification

# --- AIS attribution ---
pandas==2.2.3
zstandard==0.23.0             # read .csv.zst directly if needed

# --- OpenDrift (modules/opendrift local clone) runtime deps ---
# opendrift is imported from the cloned repo (modules/opendrift); these are the
# dependencies declared by its pyproject.toml.
matplotlib==3.10.5
netCDF4==1.7.2
pyproj==3.7.0
xarray==2025.1.0
geopandas==1.1.4
cmocean==4.0.3
geojson==3.1.0
nc-time-axis==1.4.1
coloredlogs==15.0.1
Cartopy==0.24.0
pynucos==3.0.4
utm==0.7.0
roaring-landmask>=0.7.0
pykdtree>=1.3.9
xhistogram>=0.3.2
adios_db>=1.2.0
copernicusmarine>=2.0.0
psutil>=7.0.0